# Runtime dependencies, pinned to what the production venv actually has.
# Verified against venv\Scripts\python.exe -m pip list on 2026-09-01.

requests==2.32.5
pdfplumber==0.11.9
python-bidi==0.6.7

# Transitive, pinned so a rebuilt venv matches the verified one.
pdfminer.six==20251230
pypdfium2==5.4.0
pillow==12.1.0
certifi==2026.1.4
charset-normalizer==3.4.4
idna==3.11
urllib3==2.6.3

# Dependencies that are NOT yet installed in the production venv live in
# requirements-step4.txt, so that `pip install -r requirements.txt` against the
# venv the 18:00 scheduled task runs cannot change it.

# --- development only -------------------------------------------------------
# The test suite uses unittest from the standard library, so it runs with no
# install at all:  venv\Scripts\python.exe -m unittest discover -s tests
# pytest is optional and will also collect these tests unchanged.
# pytest==8.3.4
